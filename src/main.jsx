import React from 'react'
import { createRoot } from 'react-dom/client'
import App from './App.jsx'  // 👈 must match your filename exactly
createRoot(document.getElementById('root')).render(<App />)
